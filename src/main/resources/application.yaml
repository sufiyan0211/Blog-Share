spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/BlogDb
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    generate-ddl: false
    database: mysql


server:
  port: 8091